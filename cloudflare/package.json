{
  "name": "@prism-liquidity-agent/cloudflare",
  "version": "1.0.0",
  "description": "Cloudflare Workers for Prism DLMM platform",
  "workspaces": [
    "infra"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --cwd infra dev",
    "deploy": "bun run --cwd infra deploy",
    "plan": "bun run --cwd infra plan",
    "typecheck": "tsc --noEmit && bun run --cwd infra typecheck"
  },
  "dependencies": {
    "effect": "^3.22.0",
    "hono": "^4.12.32"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260726.1",
    "@vitest/runner": "^4.1.10",
    "@vitest/snapshot": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
